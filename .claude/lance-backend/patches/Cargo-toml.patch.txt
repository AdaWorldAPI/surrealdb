# =============================================================================
#  PATCH: surrealdb/core/Cargo.toml
# =============================================================================
#
# Add the `kv-lance` feature and Lance dependencies. The pattern mirrors
# `kv-surrealkv` / `kv-rocksdb`.
#
# Insert into [features]:

[features]
# ... existing features ...
kv-lance = ["dep:lance", "dep:arrow-array", "dep:arrow-schema", "dep:hex", "dep:async-trait"]

# Insert into [dependencies] (versions correspond to lance 1.0+ released
# Dec 2025; verify against the AdaWorldAPI/lance-graph workspace pin):

[dependencies]
lance = { version = "1.0", optional = true }
arrow-array = { version = "55", optional = true }
arrow-schema = { version = "55", optional = true }
hex = { version = "0.4", optional = true }
async-trait = { version = "0.1", optional = true }

# Notes:
#  - `lance` brings in arrow-rs, datafusion, object_store, parquet, and
#    a few SIMD-related dependencies transitively. Build time goes up
#    ~30-60 seconds on first cargo check.
#  - `hex` is only used for the DataFusion predicate string-builder in
#    `schema.rs`. If you prefer to keep the dep count minimal, inline a
#    20-line hex-encoding helper.
#  - `async-trait` is already a dependency of surrealdb-core, so this
#    is just making the dep explicit for our feature.

# Optional: add a metadata aliasing for the new feature so SurrealDB's
# `--features` CLI lists it nicely:

[package.metadata.features.kv-lance]
description = "Lance versioned columnar storage backend"
